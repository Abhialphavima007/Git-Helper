# Copy to .env and set a random secret. The Azure DevOps PAT is entered in the
# UI at runtime and stored only in the server-side session, never here.
PORT=4000
SESSION_SECRET=change-me-to-a-long-random-string
