{
  "name": "git-helper-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Desktop shell (Electron) for Git Helper — full local-git experience.",
  "main": "main.cjs",
  "scripts": {
    "start": "electron ."
  },
  "devDependencies": {
    "electron": "^33.0.0"
  }
}
